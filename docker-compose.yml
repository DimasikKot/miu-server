services:
  backend:
    build: .
    container_name: backend
    restart: unless-stopped
    ports:
      - "10033:10033"
    volumes:
      - ./instances:/instances
      - ./app:/app # for hot-reloading
